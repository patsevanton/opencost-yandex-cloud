opencost:
  mcp:
    enabled: true
    port: 8081
    ingress:
      enabled: true
      ingressClassName: "nginx"
      annotations:
        nginx.ingress.kubernetes.io/ssl-redirect: "false"
      hosts:
      - host: mcp-opencost.apatsev.org.ru
        paths:
        - path: /
          pathType: Prefix
  prometheus:
    internal:
      serviceName: vmselect-vmks-victoria-metrics-k8s-stack
      namespaceName: vmks
      port: 8481
      path: "/select/0/prometheus"
      scheme: http
  customPricing:
    enabled: true
    costModel:  
      LBIngressDataCost: "0.01"
      FirstFiveForwardingRulesCost: "0.0"
      AdditionalForwardingRuleCost: "0.0"
  ui:
    ingress:
      enabled: true
      ingressClassName: "nginx"
      annotations:
        nginx.ingress.kubernetes.io/ssl-redirect: "false"
        # Allocation/report API queries VictoriaMetrics and can take >60s; default proxy-read-timeout causes 504
        nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
        nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
      hosts:
      - host: opencost.apatsev.org.ru
        paths:
        - /
